MVC_dbt_local:
  outputs:
    dev:
      type: postgres
      threads: 4
      host: pgdatabase  # <-- CORRECTED: Use service name, not localhost
      port: 5432
      user: root
      pass: root
      dbname: MVC_db
      schema: public
  target: dev